# ScaleTrade — Структура курса (v2)

## 📊 Статистика
- **16 частей** (включая Часть 0)
- **40+ модулей**
- От Junior+ до Senior уровня

---

## 🧱 Часть 0: Computer Science & Python Core (Фундамент)
*Цель: Понимать, как работает инструмент и среда, в которой он запущен.*

### Модуль 0.1: Python Internals
**Темы:**
- **Memory Model:** Reference counting, Garbage Collection (gc module), generational GC
- **GIL (Global Interpreter Lock):** Как реально работает, когда мешает (CPU-bound), когда нет (I/O-bound)
- **`__slots__`:** Экономия памяти, когда использовать
- **Mutable vs Immutable:** list vs tuple, dict vs frozenset, почему это важно
- **Asyncio Event Loop:** Как устроен, selectors, callback queue
- **Concurrency:** `Multiprocessing` vs `Threading` vs `Asyncio` — когда что использовать
- **Advanced OOP:** Метаклассы (поверхностно), дескрипторы, декораторы с аргументами

**Подводные камни:**
- GIL не мешает I/O-bound задачам — распространённое заблуждение
- `gc.disable()` не отключает reference counting
- Memory leak из-за циклических ссылок без `__del__`

**Лайфхаки:**
- `tracemalloc` для поиска memory leaks
- `objgraph` для визуализации объектов
- `sys.getsizeof()` + recursion для реального размера

### Модуль 0.2: Networking & Linux
**Темы:**
- **HTTP Protocol:** Структура request/response, идемпотентность методов
- **HTTP Status Codes:** Не только 200/404, но и 409 (Conflict), 429 (Too Many Requests), 503 (Service Unavailable)
- **Linux CLI:** grep, awk, curl (с -v, -X), htop, netstat/ss, lsof
- **Big O Notation:** Понимание сложности операций
- **Data Structures Complexity:**
  - `list`: O(1) append, O(n) insert/remove
  - `set/dict`: O(1) lookup/insert/delete
  - `deque`: O(1) append/popleft

**Подводные камни:**
- `list.insert(0, x)` — это O(n), используй `deque`
- `in` для list — O(n), для set — O(1)
- HTTP 201 vs 200 — когда что использовать

**Лайфхаки:**
- `curl -v` для дебага HTTP
- `ss -tulpn` вместо `netstat`
- `time` команда для быстрого бенчмарка

---

## 📚 Часть 1: PostgreSQL Mastery (3 модуля)
*Подробно описано в предыдущей версии*

---

## 🏗️ Часть 2: Архитектура приложения (2 модуля)
*Подробно описано в предыдущей версии*

---

## 🌐 Часть 3: Распределённые системы (2 модуля)
*Подробно описано в предыдущей версии*

---

## 🛡️ Часть 4: Надёжность (2 модуля)
*Подробно описано в предыдущей версии*

---

## 📊 Часть 5: Observability (2 модуля)
*Подробно описано в предыдущей версии*

---

## 🚀 Часть 6: Production (3 модуля)
*Подробно описано в предыдущей версии*

---

## 🔷 Часть 7: GraphQL & gRPC (3 модуля)
*Подробно описано в предыдущей версии*

---

## 🔄 Часть 8: Async Patterns & Workers (3 модуля)
*Подробно описано в предыдущей версии*

---

## 📐 Часть 9: API Design & Documentation (NEW)
*Цель: Создавать API, с которыми приятно работать.*

### Модуль 9.1: REST API Design
**Темы:**
- **Richardson Maturity Model:** Level 0-3, HATEOAS
- **HTTP Methods:** GET/POST/PUT/PATCH/DELETE — когда что
- **PUT vs PATCH:** Полная замена vs частичное обновление
- **API Versioning:**
  - URL: `/v1/orders`
  - Header: `Accept: application/vnd.api+json;version=1`
  - Query: `?version=1`
- **Deprecation Policy:** Sunset header, migration guide

**Подводные камни:**
- PUT должен быть idempotent — не инкрементируй счётчики
- POST для создания, но можно и для actions (`/orders/{id}/cancel`)
- Версионирование через Header сложнее для тестирования

**Лайфхаки:**
- Всегда возвращай полный объект после создания/обновления
- `Location` header после POST для RESTful клиентов
- ETag для conditional requests (optimistic locking)

### Модуль 9.2: Idempotency & Error Handling
**Темы:**
- **Idempotency-Key Header:** Защита от дублей транзакций
- **Implementation:** Redis + TTL для хранения ключей
- **RFC 7807 Problem Details:** Стандартный формат ошибок
- **Global Error Handler:** Централизованная обработка в Litestar

**Пример RFC 7807:**
```json
{
  "type": "https://api.scaletrade.io/errors/insufficient-funds",
  "title": "Insufficient Funds",
  "status": 400,
  "detail": "Account balance 100.00 is less than requested 150.00",
  "instance": "/orders/123",
  "balance": "100.00",
  "requested": "150.00"
}
```

**Подводные камни:**
- Idempotency key должен быть уникален per user, не глобально
- TTL для ключей: не слишком короткий (retry), не слишком длинный (storage)
- `type` в RFC 7807 должен быть URI (может быть не resolvable)

**Лайфхаки:**
- Redis `SET NX PX` для atomic idempotency check
- Храни результат операции, не только факт выполнения
- Extend TTL при каждом hit для retry scenarios

### Модуль 9.3: OpenAPI & Documentation
**Темы:**
- **OpenAPI Advanced:** Examples, Descriptions, Tags в Litestar
- **Swagger UI:** Настройка для production (отключение в prod, авторизация)
- **ADR (Architecture Decision Records):**
  - Зачем: Документируем "почему", не только "что"
  - Формат: Title, Status, Context, Decision, Consequences
- **Diagrams as Code:** Mermaid.js, C4 model

**ADR Template:**
```markdown
# ADR-001: Выбор Litestar вместо FastAPI

## Status
Accepted

## Context
Нужен async web framework с хорошим DI и типизацией.

## Decision
Используем Litestar потому что:
- Встроенный IoC контейнер
- Поддержка msgspec для сериализации
- Лучше typing support

## Consequences
- Меньше community resources чем FastAPI
- Нужно обучать команду
```

**Подводные камни:**
- Swagger UI в production — security risk (показывает все endpoints)
- OpenAPI examples должны быть валидными данными
- ADR должен быть immutable (создаём новый для изменений)

**Лайфхаки:**
- `tags` в OpenAPI для группировки по domain
- Redoc для более красивой документации
- ADR номеруем последовательно, не удаляем старые

---

## 🔧 Часть 10: Git & Engineering Culture (NEW)
*Цель: Работать в команде эффективно.*

### Модуль 10.1: Git Advanced
**Темы:**
- **Rebase vs Merge:**
  - Merge: сохраняет историю, создаёт merge commit
  - Rebase: линейная история, "переписывает" коммиты
- **Interactive Rebase:** `git rebase -i HEAD~5`
  - `pick`, `squash`, `fixup`, `reword`, `drop`
- **Cherry-pick:** Перенос отдельных коммитов
- **Stash:** `git stash push -m "WIP feature"`, `git stash pop`
- **Bisect:** Поиск коммита с багом бинарным поиском
- **Reflog:** Восстановление "потерянных" коммитов

**Подводные камни:**
- `git push --force` после rebase в shared branch — НИКОГДА
- `--force-with-lease` безопаснее, но всё равно опасно
- Rebase публичных веток = ад для коллег

**Лайфхаки:**
- `git commit --fixup=<sha>` + `git rebase -i --autosquash`
- `git stash` перед checkout другой ветки
- `git bisect run pytest tests/test_foo.py` — автоматический поиск

### Модуль 10.2: Semantic Commits
**Темы:**
- **Conventional Commits:**
  - `feat:` — новая фича
  - `fix:` — исправление бага
  - `refactor:` — рефакторинг без изменения поведения
  - `docs:` — документация
  - `test:` — тесты
  - `chore:` — обновление зависимостей и т.д.
- **Scope:** `feat(orders): add cancel endpoint`
- **Breaking Changes:** `feat!:` или `BREAKING CHANGE:` в body
- **Commitlint:** Линтер коммитов в CI
- **CHANGELOG Generation:** `standard-version`, `semantic-release`

**Пример:**
```
feat(orders): add bulk cancel endpoint

Allows canceling multiple orders in single request.
Max 100 orders per request.

BREAKING CHANGE: Cancel endpoint now returns 202 instead of 200
Closes #123
```

**Подводные камни:**
- `fix` vs `refactor` — если поведение не менялось, это refactor
- Слишком длинный subject (> 72 символов)
- Breaking change без миграции = unhappy users

**Лайфхаки:**
- Pre-commit hook с commitlint
- `git commit --amend` для исправления последнего коммита
- Squash перед merge для чистой истории

### Модуль 10.3: Code Review
**Темы:**
- **Что искать на ревью:**
  - Логические ошибки (не только стиль!)
  - Архитектурные проблемы
  - Security issues
  - Performance bottlenecks
  - Missing tests
- **Как комментировать:**
  - Конструктивно, не агрессивно
  - Предлагай решение, не только критикуй
  - `nit:` для мелочей, `suggestion:` для рекомендаций
- **PR Templates:** Checklist для автора
- **Estimation:** Story Points vs Hours, Planning Poker

**PR Template:**
```markdown
## Description
Brief description of changes

## Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change

## Checklist
- [ ] Tests added/updated
- [ ] Documentation updated
- [ ] No secrets in code
- [ ] CHANGELOG updated
```

**Подводные камни:**
- Ревью > 400 строк = пропустишь баги (cognitive overload)
- "LGTM" без комментариев = формальное ревью
- Блокирующие комментарии по стилю = трата времени (используй линтеры)

**Лайфхаки:**
- Автоматизируй всё что можно (Ruff, Mypy, tests)
- Маленькие PR (< 200 строк) = качественное ревью
- Self-review перед отправкой на ревью

---

## 🔒 Часть 11: Security (3 модуля)
*Подробно описано в предыдущей версии (была часть 13)*

---

## ☸️ Часть 12: Kubernetes (1 модуль)
*Только basics: Pods, Deployments, Services, ConfigMaps, Secrets*

---

## ⚡ Часть 13: Performance & Profiling (3 модуля)
*Подробно описано в предыдущей версии (была часть 14)*

---

## 🤖 Часть 14: ML Integration (3 модуля)
*Подробно описано в предыдущей версии (была часть 15)*

---

## 🐛 Часть 15: Debugging & Troubleshooting (3 модуля)
*Подробно описано в предыдущей версии (была часть 16)*

---

## 📋 Итого: Изменения

### Удалено (избыточно для курса):
- ❌ Часть 9: Event Sourcing & CQRS (слишком специфично)
- ❌ Часть 10: Микросервисы (API Gateway, Service Mesh, Consul)
- ❌ Часть 11: Мониторинг (Prometheus, Grafana, Jaeger)
- ❌ 12.2-12.4: Helm, ArgoCD, Istio
- ❌ Часть 17: Multi-Tenancy
- ❌ Часть 18: Real-time (WebSockets, SSE)
- ❌ Часть 19: Data Pipelines (Airflow, Dagster)
- ❌ Часть 20: Infrastructure as Code

### Добавлено (критично для Middle/Senior):
- ✅ **Часть 0:** Python Internals, Networking, Big O
- ✅ **Часть 9:** API Design, Idempotency, RFC 7807, OpenAPI
- ✅ **Часть 10:** Git Advanced, Semantic Commits, Code Review

---

## 🎯 Формат каждого модуля

```
1. Теория (30%)
   - Концепции
   - Архитектурные решения
   - Когда использовать

2. Практика (40%)
   - Код с примерами
   - Step-by-step implementation
   - Интеграция в ScaleTrade

3. Подводные камни (15%)
   - Частые ошибки
   - Антипаттерны
   - Production incidents

4. Лайфхаки (10%)
   - Best practices
   - Shortcuts
   - Готовые snippets

5. Задания (5%)
   - Практические задачи
   - Code review exercise
   - Debugging challenge
```

---

## ❓ FAQ для собеседований

В каждом модуле будет секция **"Вопросы на собеседовании"**:

**Python Internals:**
- Что такое GIL и когда он мешает?
- Как работает Garbage Collection в Python?
- Чем отличается `list` от `tuple` на уровне памяти?

**PostgreSQL:**
- Объясни уровни изоляции транзакций
- Как работает MVCC?
- Когда индекс не используется?

**Архитектура:**
- Что такое DDD?
- Зачем нужен Dependency Injection?
- Объясни Layered Architecture

**Git:**
- Разница между merge и rebase?
- Как найти коммит с багом?
- Что делать если запушил секрет?
